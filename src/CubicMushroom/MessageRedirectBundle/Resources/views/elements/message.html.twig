{% for flashMessage in app.session.flashbag.get('message_redirect.message') %}
    <div class="mr-flash-notice {{ flashClasses|default('') }}
    {{ flashClassesMap[flashMessage.messageType]|default(flashMessage.messageType) }}">
        {{ flashMessage.message }}
    </div>
{% endfor %}